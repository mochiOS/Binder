[package]
name = "binder"
version = "0.1.0"
edition = "2024"

[features]
default = ["develop"]
develop = []
mochios = []

[dependencies]
viewkit = "0.3.3"

[target.'cfg(target_os = "linux")'.dependencies]
libc = "0.2"